-- Варіант 10. 
-- Припустимо, що вектори v = (vi) є  послідовності чисел, 
-- а матриці   m = (mij) представлені як послідовності векторів (рядів матриці). 
-- Напишіть процедури для обчислення матричних операцій визначення діагональних елементів матриць

diagelem m = [k | (d, i) <- zip  m [0..], (k, n) <- zip d [0..], i == n]

